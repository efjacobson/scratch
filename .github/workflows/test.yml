name: 'log'

on:
  workflow_dispatch:
    inputs:
      foo:
        description: 'bar'
        required: true
        default: 'baz'

jobs:
  log:
    runs-on: 'ubuntu-latest'
    steps:
      - name: 'dolog'
        shell: bash -euxo pipefail {0}
        run: |
          echo "hI"
          echo "hI2"
          echo "good times"
          echo "h3"
