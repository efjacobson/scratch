on:
  pull_request:
    branches:
      - master

jobs:
  test-thing:
    if: github.event.pull_request.draft == false
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        with:
          ref: master
      - run: |
          echo "TIMESTAMP=$(date -u +%FT%R:%MZ)" >>"$GITHUB_ENV"
      - uses: actions/checkout@v3
      - run: |
          if [ "$TIMESTAMP" == "$(date -u +%FT%R:%MZ)" ]; then
            echo 'well that seems very unlikely'
            exit 1
          fi
