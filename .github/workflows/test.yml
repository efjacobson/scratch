name: 'log'

on:
  workflow_dispatch:
    inputs:
      foo:
        description: 'bar'
        required: true
        default: 'baz'

jobs:
  log:
    runs-on: 'ubuntu-latest'
    steps:
      - name: 'dolog'
        shell: bash -euxo pipefail {0}
        run: |
          echo "hI"
          sleep 1
          echo "hI2"
          sleep 1
          echo "good times"
          sleep 1
          echo "h3"
          sleep 1
